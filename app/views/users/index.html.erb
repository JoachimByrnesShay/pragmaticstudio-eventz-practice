
<hl> <%= pluralize(@users.size, "user") %>: </hl>
<ul class="users">
<% @users.each do |u| %>
	<li> <%= link_to u.name, u %> 
	created
	<%= time_ago_in_words(u.created_at) %> ago
	</li>
<% end %>
</ul>